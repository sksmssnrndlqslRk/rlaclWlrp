#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    int a;
    int q=0;
    while(1){
        int a;
        cin>>a;
        if(a==0) break;
        for(int i=0;i<a;i++){
            int t;
            cin>>t;
        }
        q++;
    }
    for(int i=1;i<=q;i++){
        cout<<"Case "<<i<<": Sorting... done!"<<"\n";
    }
    return 0;
}