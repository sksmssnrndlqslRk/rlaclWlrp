#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    string a;
    int t;
    cin>>t>>a;
    for(int i=t-5;i<t;i++){
        cout<<a[i];
    }
    return 0;
}