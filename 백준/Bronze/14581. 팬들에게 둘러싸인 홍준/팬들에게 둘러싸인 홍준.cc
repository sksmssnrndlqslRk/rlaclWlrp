#include <bits/stdc++.h>
using namespace std;

int main(){
    ios_base::sync_with_stdio(0);
    cin.tie(0);
    
    string str, fan = ":fan:";
	cin >> str;
	cout << fan << fan << fan << endl;
	cout << fan << ':' << str << ':' << fan << endl;
	cout << fan << fan << fan << endl;
    return 0;
}